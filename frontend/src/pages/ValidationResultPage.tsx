// Validation result screen (design doc 2.4, the core screen)
// Left: manuscript body (contradiction spans highlighted) / Right: list of contradiction-candidate flags (sorted by confidence)
// Per-flag actions: apply / dismiss as false positive / add missing settings then revalidate
export default function ValidationResultPage() {
  return <div>ValidationResultPage (TODO)</div>;
}
