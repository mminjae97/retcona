// Manuscript editor (design doc 2.2)
// Distinguishes auto-save (debounced, doesn't call the AI pipeline) / save / run validation (enqueues a job)
// Editing a submitted episode flips status back to draft and shows a "not up to date" banner
export default function EditorPage() {
  return <div>EditorPage (TODO)</div>;
}
